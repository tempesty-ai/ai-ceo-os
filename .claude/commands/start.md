# /start — 주간 루틴 전체 실행

아래 3개 커맨드를 순서대로 실행하세요:

1. `/start:weekly-start` 실행 — 레퍼런스 분석 + 이번 주 방향 결정
2. `/start:content-batch` 실행 — 대본 3개 + 캡션 배치 생성
3. `/start:weekly-review` 실행 — KPI 체크 + 회고 + 다음 주 힌트

각 단계가 완료된 후 다음 단계로 넘어갑니다.
